# Frontend runtime
VITE_APP_NAME=Tunez
VITE_APP_ENV=development

# Local development backend URL only.
# Do not set VITE_API_BASE_URL in Vercel production; production uses same-origin /api/* rewrites.
VITE_API_BASE_URL=http://localhost:3000

# Optional feature flags
VITE_ENABLE_DEBUG_TOOLS=true
